const add = (a, b) => a + b;
add(10, 25);

// const imgWrapper = document.querySelector('.img-wrapper');
// const imgRef = '../img.png';
// const imgEl = document.createElement('img');
// imgEl.src = imgRef;
// imgEl.classList.add('img');
// console.log(imgEl);
// imgWrapper.insertAdjacentElement('beforeend', imgEl);

export default 5;
